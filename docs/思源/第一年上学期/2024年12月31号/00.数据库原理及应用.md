# 数据库原理及应用

- 时间：8:10 - 10:40
- 地点：西楼 - 507

## 第一大题（共 20 分）

场景一：环保产品的商品管理和销售的数据库系统

### 系统构成

**第一小题（10 分）**

> 数据库系统由哪些部分构成？分别描述其功能。

**内容**

- **数据库系统的组成**：
  1. **计算机硬件系统**：提供数据库系统运行的物理基础，包括服务器、存储设备、网络设备等，确保系统稳定高效运行。
  2. **计算机软件系统**：
     - **操作系统**：提供数据库运行的软件环境，负责资源分配与进程调度。
     - **数据库管理系统（DBMS）**：核心软件，负责存储、检索、更新与管理数据；DBMS 提供了一系列工具和功能，如数据定义语言 DDL 数据操作语言 DLL 数据控制语言 DCL 等，以支持数据库的创建、查询、更新和删除操作。
     - **应用开发工具**：用于开发数据库应用的工具，如编程语言 Java、Python，开发框架 Spring、Django 等。
     - **应用系统**：面向用户的前端程序，负责实现具体功能，如供应商管理、商品管理、订单管理、客户管理等，通过调用 DBMS 提供的接口与数据库进行交互，实现数据的增删改查操作。
  3. **数据库**：存储系统所有的核心数据，如供应商信息、商品信息、订单信息等；数据库是系统的数据仓库，为其他部分提供数据支持。
  4. **用户**：数据库系统的使用者，如供应商、普通客户、系统管理员等，通过应用系统访问数据库进行数据的录入、查询和修改等操作。

### 实体与属性分析

**第二小题（10 分）**

> 针对题目给出的场景，分析系统的实体及其属性。

**内容**  

- **商品实体（Product）**：  
  - 属性：商品ID、名称、类别、价格、库存量、环保等级、是否有机、是否可回收。
- **供应商实体（Supplier）**：  
  - 属性：供应商ID、名称、联系方式、地址、环保认证情况。
- **订单实体（Order）**：  
  - 属性：订单ID、客户ID、下单时间、订单状态、支付状态、环保商品标识。
- **客户实体（Customer）**：  
  - 属性：客户ID、姓名、联系方式、地址。
- **订单详情实体（Shipment）**：  
  - 属性：订单ID、商品ID、购买数量、单价。

## 第二大题（共 30 分）

### ER 图设计

**第一小题（10 分）**

> 根据分析出的实体及其属性，绘制ER图。  

**内容**  

- ER图需包含以下关系：
  - 供应商与商品：多对多。
  - 商品与订单：多对多。
  - 客户与订单：一对多。
- 图中需标明联系类型（如一对多、多对多），属性无需画出。

![20241227192711](https://img.lisir.me/image/pages/20241227192711.png)

### 关系模式设计

**第二小题（8 分）**

> 将 ER 图转换为关系模式，并判断是否满足第三范式。
>
> 主键使用下划线，外键使用波浪线

**内容**

- **关系模式**：
  - 商品（Product）：商品ID（主键）、名称、类别、价格、库存量、环保等级、是否有机。
  - 供应商（Supplier）：供应商ID（主键）、名称、联系方式、地址、环保商品标识。
  - 客户（Customer）：客户ID（主键）、姓名、联系方式、地址。
  - 订单（Order）：订单ID（主键）、客户ID（外键）、下单时间、订单状态、支付状态。
  - 订单详情（Shipment）：订单ID（主键，外键）、商品ID（主键，外键）、购买数量、单价。
  - 供应表：供应商ID（主键，外键）、商品ID（主键，外键）、供应日期。
- 是否满足第三范式
  - 在上述的关系模式中均不存在部分依赖和传递依赖，转换出的关系模式满足第三范式。

### SQL 语句实现

**第三小题（12 分）**

> 根据设计的关系模式，编写 SQL 语句创建表结构。

**内容**  
根据关系模式，为每个表补充完整的SQL创建语句，如下：  

1. **商品表（Product）**

    ```sql
    create table Product (
        ProductID int primary key,
        Name nvarchar(255) not null,
        Category nvarchar(255),
        Price decimal(18, 2) not null,
        Stock int not null,
        EcoLevel nvarchar(255),
        IsOrganic bit
    );
    ```

2. **供应商表（Supplier）**

    ```sql
    create table Supplier (
        SupplierID int primary key identity(1,1),
        Name nvarchar(255) not null,
        Contact nvarchar(255),
        Address nvarchar(255),
        EcoCert nvarchar(255)
    );
    ```

3. **客户表（Customer）**

    ```sql
    create table Customer (
        CustomerID int primary key identity(1,1),
        Name nvarchar(255) not null,
        Contact nvarchar(255),
        Address nvarchar(255)
    );
    ```

4. **订单表（Order）**

    ```sql
    create table OrderTable (
        OrderID int primary key identity(1,1),
        CustomerID int not null,
        OrderDate datetime not null default getdate(),
        OrderStatus nvarchar(50),
        PaymentStatus nvarchar(50),
        foreign key (CustomerID) references Customer(CustomerID)
    );
    ```

5. **订单详情表（Shipment）**

    ```sql
    create table Shipment (
        <!-- ShipmentID int primary key identity(1,1), -->
        OrderID int,
        ProductID int,
        Quantity int not null,
        UnitPrice decimal(18, 2) not null,
        primary key (OrderID, ProductID),
        foreign key (OrderID) references OrderTable(OrderID),
        foreign key (ProductID) references Product(ProductID)
    );
    ```

6. **供应表（Supply）**

    ```sql
    create table Supply (
        <!-- SupplyID int primary key identity(1,1), -->
        SupplierID int,
        ProductID int,
        SupplyDate date not null,
        primary key (SupplierID, ProductID),
        foreign key (SupplierID) references Supplier(SupplierID),
        foreign key (ProductID) references Product(ProductID)
    );
    ```

## 第三大题（共 30 分）

场景二：学校开发点餐服务系统，方便管理的同时还要为实现为全校学生伙食费进行部分补贴，为经济困难的学生伙食费进行限额补贴，并能够完成学校财务与食堂的结算功能。
然后会给你三个表
第一个是学生信息表，里头包含了学号姓名性别，班级是否为贫困生，然后备注备注就是那个啥备注字段是可控字段。就前面主要学生姓名性别班级是否为贫困生，因为这个涉及补贴，这个重点是这个。
第二个是菜单表，菜单表里头是菜品编号，菜名价格，
第三个是订单表，然后是学号，然后菜品编号就相当于把前两个表情就哪个学生点了，哪个菜菜品编号，然后点餐时间，点餐学期是否完成订单给你这几个表，这个表给了你一些，这些说明说明后面会加会有这个它数据类型是啥，然后是否为空值，然后是否为主键，然后前面有那个列名英文名字就是这个student ID什么什么就跟刚才一样，这个字段名字都写有。

### 数据库查询与插入操作

**第一小题（20 分）**  

1. **根据菜品名称查询菜单编号（4 分）**

   ```sql
   select menu_id
   from t_menu
   where dish_name = '鱼香肉丝盖饭';
   ```

2. **向订单表中插入记录**

    ```sql
    insert into t_orders (student_id, dish_id, [点餐时间], [点餐学期], [是否完成订单])
    values ('SY2024001', 1851, '2024-12-30', '2024-2025-1', false);
    ```

3. **违反参照完整性约束时的错误处理**  
   - 如果学生表中没有 `student_id` 或菜单表中没有 `menu_id`，插入操作将失败，因违反参照完整性约束。

4. **创建存储过程实现插入操作**

    ```sql
    create procedure insert_order
        @StudentID nvarchar(255),
        @MenuID int,
        @OrderTime datetime,
        @Term nvarchar(50)
    as
    begin
        insert into Orders (StudentID, MenuID, OrderTime, Term, Completed)
        values (@StudentID, @MenuID, @OrderTime, @Term, false);
    end;
    ```

    create 后面加 procedure

5. **删除订单**  

   - 示例代码：  

     ```sql
     delete from Orders
     where StudentID = 'SY2024001' and MenuID = 1851 and Completed = false;
     ```

6. **更新菜单价格**  

   - 示例代码：  

     ```sql
     update Menu
     set Price = Price * 1.1;
     ```

### 贫困生查询与补贴

**题目：设计查询语句与视图**

1. **查询符合条件的贫困生**

   - 统计上学期点餐次数超过 300 次，且总费用小于 3000 元的学生：  

     ```sql
     select StudentID, count(*) as OrderCount, sum(Price) as TotalAmount
     from Orders
     join Menu on Orders.MenuID = Menu.MenuID
     where Term = '2023-2024'
     group by StudentID
     having count(*) >= 300 and sum(Price) < 3000;
     ```

2. **创建视图显示补贴信息**  

   - 非贫困生支付 92% 的菜品价格，剩余部分由学校补贴：  

     ```sql
     create view NonPoorSubsidy as
     select StudentID, sum(Price * 0.08) as SubsidyAmount
     from Orders
     join Menu on Orders.MenuID = Menu.MenuID
     where IsPoor = false
     group by StudentID;
     ```

## 第四大题（共 20 分）

### 备份与恢复

**题目：编写备份与恢复的 SQL 语句**

1. **完整备份**  

   ```sql
   backup database SchoolDB
   to disk = 'C:\Backup\FullBackup.bak'
   with init;
   ```

2. **差异备份**

   ```sql
   backup database SchoolDB
   to disk = 'C:\Backup\DiffBackup.bak'
   with differential;
   ```

3. **恢复操作**

   - 恢复完整备份：  

     ```sql
     restore database SchoolDB
     from disk = 'C:\Backup\FullBackup.bak'
     with norecovery;
     ```

   - 恢复差异备份：  

     ```sql
     restore database SchoolDB
     from disk = 'C:\Backup\DiffBackup.bak'
     with recovery;
     ```

### 简答题

**题目：简述备份类型及其使用场景**

1. **完整备份与差异备份的区别**  
   - **完整备份**：备份整个数据库，独立存在，不依赖于其他备份。  
   - **差异备份**：备份自上次完整备份以来的所有变化数据，依赖最近的完整备份。

2. **差异备份的适用场景**  
   - 当数据量较大且变更范围较小时，使用差异备份可减少存储需求并加快恢复速度。
